
/*use cluuname::core::UnameErr;
use cluuname::rs_uname;
use cluuname::uname;*/

fn main() {}

/* +feature = "std"
fn main() -> Result<(), UnameErr> {
	let a_uname = rs_uname()?; // cowstring uname's
	println!("{}", a_uname.get_hash_version());
	
	let a_uname = uname()?; // def rawarray uname's
	println!("{}", a_uname.get_hash_version());
	
	Ok(())
}*/
